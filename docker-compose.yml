services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: nock
      POSTGRES_USER: nock
      POSTGRES_PASSWORD: nock
    ports:
      - "5432:5432"
    volumes:
      - nock_pg:/var/lib/postgresql/data

volumes:
  nock_pg:
